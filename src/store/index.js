import { createStore } from 'vuex'

// 2.0 创建仓库 new Vuex.Store({})
// 3.0 创建仓库 createStore({})
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
